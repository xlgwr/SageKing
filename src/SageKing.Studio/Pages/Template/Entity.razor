@namespace SageKing.Studio.Pages.Template

namespace @Model.NameSpace;

/// @("<summary> ")
/// @Model.Description
/// @("<summary> ")
public class @(@Model.ClassName) @Model.BaseClassName
@("{\n")
@foreach (var column in Model.ColumnList)
{
@:/// @("<summary> ")
@:/// @(column.ColumnComment)
@:/// @("<summary> \n")
@if (!column.NetType.GetDescription().IsNullOrEmpty())
{
@:public @column.NetType.GetDescription() @column.PropertyName { get; set; }
@:
}
else
{
@:public string @column.PropertyName { get; set; }
@:
}
}
@("\n}\n") 

@code {
    [Parameter]
    public DefaultViewEngine Model { get; set; }
}
